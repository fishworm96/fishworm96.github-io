---
title: 内置方法
date: 2020-11-19 21:18:56
categories: 
tag: 手写
---



# Math方法：

max：

```
var math = {
    max: function () {
        var max = arguments[0]
        for (var i = 1; i < arguments.length; i++) {
            if (arguments[i] > max) {
                max = arguments[i]
            }
        }
        return max
    }
}
```

min:

```
var math = {
    min: function () {
        var min = arguments[0]
        for (var i = 1; i < arguments.length; i++) {
            if (arguments[i] < min) {
                min = arguments[i]
            }
        }
        return min
    }
}
```

# Arrary:

reverse:

```
function reverse(arr) {
	if (arr instanceof Array) {
		var newArr = []
	for (var i = arr.length -1; i--) {
		newArr[newArr.length] = arr[i]
		}
	return newArr
	} else {
		return 'not Array'
	}
}
```

# String:

indxOf判断字符串出现次数

```
var  str = 'oabsdfajkosdfaso'
var index = str.indexOf('o')
var num = 0
while (index !== -1) {
	num++
	index = str.indexOf('o', index + 1)
}
```

charAt统计出现的字母的次数和出现最多的字母


```javascript
        // 判断一个字符串 'abcoefoxyozzopp' 中出现次数最多的字符，并统计其次数
        // 核心算法：利用 charAt() 遍历这个字符串
        // 把每个字符都储存给对象，如果对象没有该属性，就为1，如果存在了就 +1
        // 遍历对象，得到最大值和该字符
        var str = 'abcoefoxyozzopp';
        var o = {};
        for (var i = 0; i < str.length; i++) {
            var chars = str.charAt(i) //chars是字符串的每一个字符
            if(o[chars]) { // o[chars] 得到的是属性值
                o[chars]++;
            } else {
                o[chars] = 1;
            }
        }
        console.log(o);
        var ch = '';
        //2.遍历对象
        var max = o;
        for (var k in o) {
            //k 得到的是属性名
            // o[k] 得到的是属性值
            if(o[k] > max) {
                max = o[k]
                ch = k;
            }
        }
```
